import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  todo: {
    flex: 1,
    flexDirection: "row"
  },
  todoText: {
    width: "90%",
    color: "#666"
  },
  iconColor: {
    color: "#666"
  }
});

export default styles;
